{
  "name": "nopban-be",
  "version": "0.1.0",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "dev:be": "nodemon",
    "dev:fe": "cd fe && yarn start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:be": "tsc",
    "build:fe": "d fe && yarn --prod=false && yarn build",
    "build:db": "npx knex migrate:latest",
    "build": "yarn build:db && yarn build:be && yarn build:fe",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "bcrypt": "^3.0.7",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "knex": "^0.20.15",
    "pg": "^7.14.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^3.0.0",
    "@types/body-parser": "^1.19.0",
    "@types/cors": "^2.8.6",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.6",
    "@types/jsonwebtoken": "^8.5.0",
    "@types/node": "^14.0.1",
    "nodemon": "^2.0.1",
    "ts-node": "^8.10.1",
    "typescript": "^3.9.2"
  },
  "engines": {
    "node": "10.x"
  }
}
