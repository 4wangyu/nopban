{
  "name": "nopban-be",
  "version": "0.1.0",
  "private": true,
  "main": "be/index.js",
  "engines": {
    "node": "11.x"
  },
  "scripts": {
    "start": "node be/index.js",
    "start:dev": "nodemon be/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd fe && yarn && yarn build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^7.14.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
